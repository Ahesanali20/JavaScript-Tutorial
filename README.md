// ===== JAVASCRIPT LEARNING PATH - BASIC TO ADVANCED =====

/*
  JS Folder Structure:
  
  01_hello.js - BASIC INTRO
    - console.log() introduction
    - Simple output
  
  02_variables_datatypes.js - FOUNDATIONS
    - Variables (var, let, const)
    - Data types (number, string, boolean, null, undefined, symbol)
    - Type checking
  
  03_operators.js - OPERATORS
    - Arithmetic operators (+, -, *, /, %, **)
    - Comparison operators (>, <, ===, !==)
    - Logical operators (&&, ||, !)
    - Assignment operators (+=, -=, etc.)
    - Ternary operator (?:)
  
  04_conditionals.js - CONTROL FLOW
    - if-else statements
    - else if
    - Nested if-else
    - switch-case
    - Multiple conditions
  
  05_loops.js - ITERATION
    - for loops
    - while loops
    - do-while loops
    - for-of loops
    - for-in loops
    - break and continue
    - Nested loops
  
  06_functions.js - FUNCTIONS
    - Function declaration
    - Function expressions
    - Arrow functions
    - Default parameters
    - Return values
    - Higher-order functions
    - Recursive functions
    - Rest parameters
  
  07_arrays.js - ARRAYS BASICS
    - Array declaration
    - Accessing elements
    - Array methods (push, pop, shift, unshift)
    - slice, concat, reverse, sort
    - Spread operator
  
  08_objects.js - OBJECTS BASICS
    - Object creation
    - Accessing properties (dot notation, bracket notation)
    - Nested objects
    - Methods in objects
    - Object.keys(), Object.values(), Object.entries()
    - Destructuring
    - Object.freeze()
  
  09_array_methods.js - ADVANCED ARRAY METHODS
    - map() - transform elements
    - filter() - select elements
    - reduce() - aggregate values
    - forEach() - iterate
    - find() - find first match
    - findIndex() - find index
    - some() - test condition
    - every() - test all elements
    - Method chaining
  
  10_callbacks.js - CALLBACKS & HIGHER-ORDER FUNCTIONS
    - Callback functions
    - Passing functions as arguments
    - Returning functions
    - Function composition
    - setTimeout callbacks
    - Callback hell
    - Error handling with callbacks
  
  11_closures.js - CLOSURES
    - Function scope and closures
    - Data privacy with closures
    - Counter pattern
    - Function factories
    - Memoization
    - Closure in loops (var vs let)
  
  12_promises_async.js - PROMISES & ASYNC-AWAIT
    - Promise basics (resolve, reject)
    - Promise chaining
    - .then() and .catch()
    - Promise.all()
    - async functions
    - await keyword
    - Error handling in async
    - Real-world API examples
  
  13_classes.js - ES6 CLASSES
    - Class declaration
    - Constructor
    - Methods
    - Inheritance (extends)
    - super keyword
    - Static methods
    - Getters and Setters
    - Private fields (#)
    - Polymorphism
  
  14_strings.js - STRING MANIPULATION
    - String basics
    - Case conversion
    - String searching (indexOf, includes, etc.)
    - Substring extraction
    - split() and join()
    - Trimming whitespace
    - Template literals
    - Regular expressions
  
  15_error_handling.js - ERROR HANDLING
    - try-catch blocks
    - finally block
    - Error types
    - Throwing custom errors
    - Custom error classes
    - Async error handling
    - Validation
    - Error logging
  
  16_advanced_concepts.js - ADVANCED ES6+ FEATURES
    - Destructuring objects and arrays
    - Spread operator (...)
    - Rest parameters
    - Optional chaining (?.)
    - Nullish coalescing (??)
    - Object.assign()
    - Computed property names
    - Method chaining
    - Deep vs shallow copy
  
  17_this_context.js - THIS KEYWORD & CONTEXT
    - Understanding 'this' keyword
    - This in different contexts
    - call(), apply(), bind()
    - Constructor functions
    - Prototypes
    - Prototype chain
    - This in classes
    - This in arrow functions
  
  18_json_data.js - JSON & DATA MANIPULATION
    - JSON.stringify()
    - JSON.parse()
    - Replacer and reviver functions
    - Type conversion
    - Error handling
    - Real-world examples
    - Deep copy with JSON

*/

// HOW TO USE THIS LEARNING PATH:
// 1. Start with 01_hello.js and run each file progressively
// 2. Each file builds on concepts from previous files
// 3. Run files with: node filename.js
// 4. Modify and experiment with the code to understand better
// 5. Practice each concept before moving to the next

// QUICK REFERENCE:
console.log("===== JAVASCRIPT LEARNING PATH COMPLETE =====");
console.log("Total files: 19 (including 01_hello.js)");
console.log("Topics covered: Basic to Advanced");
console.log("\nTo get started, run each file in sequence:");
console.log("node 01_hello.js");
console.log("node 02_variables_datatypes.js");
console.log("... and so on");
